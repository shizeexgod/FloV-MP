# .NET
bin/
obj/
*.user
*.suo

# Node / JS tooling (client NUI, if a build step gets added later)
node_modules/
dist/

# Secrets — never commit
secrets.local.md
*.env
appsettings.*.local.json

# OS junk
.DS_Store
Thumbs.db

# --- FloV:MP ---
# Собранный рантайм сервера: бинарники alt:V (движок, не наш исходник) +
# опубликованный C#-геймод + скопированные ресурсы. Собирается скриптом
# scripts/assemble-runtime.ps1 из C:\ViMP backup\backup-altv. В git не идёт.
/runtime/

# Логи запусков сервера, если кто-то запустит вне /runtime/
*.log

# Временные утилиты и локальные хранилища
/tools/
/IsolatedStorage/

# Скриншоты визуальной проверки лаунчера (test-visual.js) — генерируются локально
launcher/electron/pw_*.png

