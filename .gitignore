# .NET
bin/
obj/
*.user
*.suo

# Node / JS tooling (client NUI & Web portal)
node_modules/
dist/
.next/
out/

# Secrets — never commit
secrets.local.md
*.env
appsettings.*.local.json

# OS junk
.DS_Store
Thumbs.db

# --- FloV:MP ---
# Собранный рантайм сервера: бинарники alt:V (движок, не наш исходник) +
# опубликованный C#-геймод + скопированные ресурсы. Собирается скриптом
# scripts/assemble-runtime.ps1 из C:\ViMP backup\backup-altv. В git не идёт.
/runtime/

# Логи запусков сервера и лаунчера
logs/
*.log

# Временные утилиты, кэши и дампы
/tools/
/IsolatedStorage/
/.server-crashes-cache/
/cache/

# Скриншоты визуальной проверки лаунчера
launcher/electron/tests/screenshots/
launcher/electron/pw_*.png
