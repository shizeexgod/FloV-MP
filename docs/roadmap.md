# FloV:MP — план развития

Архитектурный референс для транспорта, replicated metadata, кастомных
nametag, lifecycle игроков и доставки ресурсов: [аудит SayonaraRP](SAYONARA_REFERENCE_AUDIT.txt).

Составлено 23.09.2026, после того как MVP собран и продан первому клиенту.
Порядок внутри фаз — по убыванию пользы на единицу работы. Оценки в рабочих
днях одного разработчика, «д» = день.

---

## Где мы сейчас (26.09.2026)

Обозначения: `✅ готово` — сделано и проверено; `🟡 код готов, в игре не
проверено` — написано и покрыто тестами, но живого прогона в GTA ещё не было;
`🟡 частично` — сделана часть пункта (что именно — в тексте пункта);
`⬜ не начато`; `⏸` — ждёт решения владельца или отложено им.
`✖ снято` — владелец решил, что это не задача мультиплеера.

**Граница продукта (владелец, 26.09):** FloV:MP — мультиплеер, как RAGE:MP:
красивая загрузка, нагрузка (цель — 2000 игроков на сервер), синхронизация и
все функции, на которых строятся серверы. Внешне для игроков и владельцев —
1 в 1 как RAGE:MP, код свой. Фичи RP-серверов (админ-панели, фракции,
инвентари, своя карта) — не наша задача; наша — чтобы владелец сервера мог
сделать их сам и полностью изменить весь интерфейс. Примеры архитектуры —
RAGE:MP и MiNT.

| № | Пункт | Статус |
|---|---|---|
| 1 | Автопереподключение | ✅ |
| 2 | Своя карта на M | ✖ снято: хватает штатной карты на Esc, как в RAGE:MP |
| 3 | Загрузочный экран и меню | ✅ |
| 4 | Раздача модов и карт | 🟡 в игре не проверено |
| 5 | Докачка пакетов | ✅ |
| 6 | Панель администратора в игре | ✖ снято: это фича RP-сервера, не мультиплеера |
| 7 | Античит, вторая линия | ✅ |
| 8 | Серверный транспорт | 🟡 нет двухклиентского теста |
| 9 | Интерьеры и IPL | 🟡 в игре не проверено |
| 10 | Сохранение игрока | ✅ |
| 11 | Метрики и оповещения | ✅ |
| 12 | Отчёты о падениях клиента | 🟡 настоящий вылет не проверен |
| 13 | Веб-панель лицензий | 🟡 сайт и API есть, редизайн идёт |
| 14 | Обновления по каналам | 🟡 подписанные релизы есть, каналов и отката нет |
| 15 | Лаунчер игрока | ⏸ отложен владельцем |
| 16 | SDK и документация | 🟡 шаблон и справка есть, примеров геймодов нет |
| 17 | Перенос с RAGE:MP/alt:V | 🟡 клиентский API RAGE есть, переносчика нет |
| 18–21 | Масштаб: нагрузка, зоны, голос по каналам, античит-3 | ⬜ |
| 22 | Перехват урона / огня и взрывов | 🟡 урон ✅, огонь и взрывы ⏸ |
| 23 | Асинхронные обработчики SDK | ✅ |
| 24 | Межресурсный слой | 🟡 экспорты ✅, метаданные и маркеры для игрока ⬜ |
| 25 | .NET 10 | ✅ |
| 26a | Клиентский JS и client_packages | ✅ |
| 26b | Браузеры (CEF) | 🟡 тесты 56/43, в GTA не проверено |
| 26c | Серверный JS как в RAGE:MP | ⬜ |
| 27 | Кость в `flovmp:damage` | ⬜ |
| 28 | Резервный сервер лицензий | ⬜ |
| 29 | Лицензия проверяется на стороне игрока | ✅ |
| 30 | Транспорт шлюза 3889 по UDP | ⬜ — сеть текущего VDS режет TCP |

**Главный блокер сейчас — живые проверки в GTA 1.0.3889**, а не код:
моды (4), транспорт двумя клиентами (8), интерьеры (9), браузеры (26b).
Без них нельзя честно писать «полная поддержка». Порядок и сценарии —
`docs/HANDOFF-CLAUDE-FIXES-2026-09-26.txt`, раздел 3.

---

## Что уже есть (MVP, версия 1.0.5 - Beta)

| Слой | Состояние |
|---|---|
| Сервер | C#/.NET 10 на движке alt:V 16.4.39, автономно, без чужого бэкенда |
| Клиент | свой ASI для GTA V Legacy 1.0.3889.0 (ScriptHookV + ImGui), протокол FLOV/2 |
| Вход | заход сразу в мир, без стартовой страницы GTA; свой загрузочный экран |
| Синхронизация | состояние 20 раз/с, интерес по радиусу, частота по дальности, экстраполяция |
| Бой | попадания с проверкой на сервере, лимиты урона, засчёт убийств по журналу |
| Транспорт | синхронизация машины, места, наклона, двигателя, сирены; настройка мощности |
| Голос | UDP, радиус, сетка ячеек, мут по идентификатору |
| Чат | команды, подсказки, RP-команды, радиус слышимости, права из базы |
| Админка | 30+ команд, уровни доступа файлом, баны по HWID/MAC/IP, модерация |
| Мир | объекты, метки, маркеры, 3D-надписи, NPC; карты Menyoo XML и JSON; 20 000 объектов за 0,4 с |
| Геймод | папка `gamemode` на C#, события платформы, своя база и миграции |
| Лицензия | RSA-3072, MariaDB на VDS, статусы, лимит серверов и слотов, 24 ч офлайна |
| Поставка | подписанные релизы, установка в один клик и одной командой, обновление на месте |
| Меню Esc | штатное меню GTA (карта, настройки, статистика) поверх живого мира, без паузы |
| Урон | здоровье и броню считает сервер; заявка клиента принимается, только если она ниже |
| SDK | управление игроками 3889 из ресурса: телепорт, здоровье, броня, модель, оружие, кик, чтение состояния, событие выхода |

---

## Фаза 1 — то, что клиент заметит первым (2–3 недели)

1. `✅ готово` **Автопереподключение клиента** (1 д). Сейчас после перезапуска сервера
   игрок видит «соединение потеряно» и жмёт F9. Нужно: 5 попыток с ростом
   паузы, окно «переподключение…», сохранение позиции.
   **Сделано:** клиент сам переподключается после потери связи (5 попыток с
   растущей паузой); отключение по кику или бану переподключения не вызывает.
2. `✖ снято владельцем` **Своя карта на M** (3 д). Штатная карта — часть меню паузы, то есть
   останавливает мир. Нужна своя: миникарта в полный экран, метки сервера,
   игроки, точка маршрута.
3. `✅ готово` **Дизайн загрузочного экрана и меню** (3 д). Сейчас функционально, но
   сыро: нужен фирменный стиль (тёмная тема, акцент `#ff3d8a`), прогресс
   загрузки ресурсов, подсказки, логотип сервера.
   **Сделано (25.09, с владельцем):** экран загрузки в фирменном стиле —
   фон на весь экран, логотип, этап и настоящий прогресс (в том числе
   скачивание client_packages); меню Esc платформы поверх живого мира.
4. `🟡 код готов, в игре не проверено` **Раздача модов и карт игрокам** (5 д). Главное ограничение сегодня:
   свои модели (карта Москвы, машины, здания) игрок должен ставить вручную.
   Нужно: сборка модов на сервере, список файлов с хэшами, докачка клиентом
   в свою папку, проверка целостности, откат. Это открывает весь кастом.
   **Ход (24.09):** серверная часть — **сделано**: `server/mods` (раскладка
   GTA\mods) → список с SHA-256 (кэш хэшей между запусками), раздача по HTTP
   с докачкой (Range, ETag, keep-alive, лимиты подключений), только данные
   игры — .asi/.dll/.exe и скрипты не раздаются никогда; `mods.public_url`
   для своего CDN; строка MODS клиенту; консоль `mods`, `reloadmods`.
   Клиентская часть — **сделано** (решения владельца 1а/2а/3/4/5 от 24.09):
   `client-b3889/mods-sync.ps1` из `play.cmd` до запуска игры — докачка с
   проверкой SHA-256 и отпечатка, те же правила путей, что у сервера; свои
   моды игрока — в `mods.player-<дата>`, возврат при удалении клиента; в
   игре — сверка по строке MODS, при `mods.required` — отказ с объяснением.
   OpenIV.asi отдельно не распространяется (ставит программа OpenIV) —
   play.cmd проверяет его и объясняет, как поставить. В игре не проверено.
5. `✅ готово` **Докачка пакетов кусками** (1 д). У части провайдеров большие файлы
   обрываются; докачка по диапазонам решает это и ускоряет обновления.
   **Сделано (24.09):** `get.sh` и `get.ps1` докачивают пакет с места обрыва
   (HTTP Range — GitHub и nginx раздачи), до 8 попыток с растущей паузой;
   сервер без Range — качают заново целиком. Проверено на сервере, который
   рвёт загрузку на трети (Linux и PowerShell 7).
6. `✖ снято владельцем` **Панель администратора в игре** (3 д). Сейчас всё командами. Нужно окно:
   список игроков, действия, журнал, поиск по нику и ID.

## Что показал разбор SayonaraRP (RAGE:MP) — 23.09.2026

Разбор чужого RP-проекта подтвердил наши приоритеты и добавил конкретику.
Переносим архитектуру и контракты, не код и не ассеты.

- **Транспорт — серверная сущность, а не состояние водителя.** Постоянный ID,
  номер, владелец, здоровье, топливо, пробег, двери, замки, тюнинг; отдельно
  persistent / runtime / реплицируемое состояние. Это пункт 8 ниже, и он
  остаётся самым крупным архитектурным долгом: у нас `IPlayer.Vehicle` для
  игрока 3889 всё ещё `null`.
- **Снимок при stream-in + дельты.** Вошёл в зону видимости — полный снимок,
  дальше только изменения, при повторном входе снимок заново. Лечит
  пропадающие машины, неверные двери и сброс ников.
- **Синхронизируемые metadata.** Сейчас `SetLocalMetaData` у нативного игрока
  остаётся в словаре сервера и до других игроков не доходит. Нужны
  synced-metadata со снимком и дельтами — на них строятся свои ники, полоски
  HP/брони, значки голоса, AFK и мута.
- **Единый жизненный цикл игрока**: connecting → ready → stream in/out →
  вход/выход из транспорта → смерть → disconnect, со своей областью очистки
  (таймеры, подписки, временные сущности). Половина уже есть событиями, нужен
  общий контракт.
- **Рассылка в радиусе** (`EmitInRange`) с проверками измерения, дистанции,
  размера и частоты — для звуков, анимаций и дверей.
- **Боевой журнал на сервере.** Часть сделана сегодня (сервер считает HP и
  броню, объявляет смерть). Осталось: учёт выданного оружия и патронов,
  множители по частям тела, отбраковка повторных HIT.
- **Чего там нет:** сетевого ядра RAGE:MP, интерполяции, ownership migration,
  хуков GTA и готовой раздачи модов — это мы делаем сами.

Полный разбор: `docs/SAYONARA_REFERENCE_AUDIT.txt`.

## Фаза 2 — качество и доверие (3–4 недели)

7. `✅ готово` **Античит: вторая линия** (4 д). Сейчас проверяются движение, урон,
   оружие, частота, а здоровье и броню сервер уже считает сам. Добавить:
   белый список моделей и оружия, учёт выданных патронов, детект ускорения
   времени, журнал подозрений с весами вместо мгновенных кик-решений.
   **Сделано (24.09):** журнал подозрений с весами и затуханием, куда сходятся
   все проверки; чёрный/белый списки оружия, моделей персонажа и машин;
   учёт выданного оружия и патронов; детект ускорения часов клиента;
   `/ac`, `/acforgive`; события и API для геймода. Всё настраивается
   владельцем (раздел «Античит» в client.cfg).
8. `🟡 код готов, в игре не проверено` **Владение транспортом на сервере** (5 д). Сегодня машина живёт в игре
   владельца; при его выходе она исчезает у остальных. Нужен серверный
   реестр машин: владелец, состояние, повреждения, замки, передача владения
   при выходе — база для автопарка RP-сервера.
   **Ход (24.09):** 8a — серверное ядро, протокол и боты — **сделано**
   (линия 1.0.6-beta, `docs/vehicle-registry-protocol.md`); 8b — клиент —
   **код готов**, в игре не проверен; 8c — публичный API для геймода
   (`flovmp:vehicle:*`, `IPlayer.Vehicle`) — **сделано**; 8d — сохранение
   в базу (`vehicles`, миграция 003, файл без базы) — **сделано**. Всё
   поведение транспорта настраивается владельцем: `client.cfg` или
   `flovmp:settings:set` из кода.
9. `🟡 код готов, в игре не проверено` **Интерьеры и IPL** (3 д). Загрузка интерьеров по координатам, чтобы
   работали магазины, квартиры, участки полиции.
   **Сделано (24.09):** IPL по имени и наборы оформления интерьера по
   координатам — из client.cfg, JSON-карт и кода геймода; карта сетевой
   игры (`world.mp_map`); при отключении клиент возвращает карту как была.
   В игре не проверено; хэши 11 новых нативов — сверить генератором по SDK.
10. `✅ готово` **Сохранение игрока** (3 д). Позиция, здоровье, броня, оружие, инвентарь
    в базе; восстановление при входе. Сейчас это пишет каждый геймод сам.
    **Сделано (24.09):** состояние игрока (место, здоровье, броня, модель,
    оружие по учёту сервера) — таблица `players`, без базы — файл;
    хранилище «ключ → значение» для геймода (`player_data`, инвентарь и
    деньги без своего кода БД); что восстанавливать — настройки
    `players.restore_*`.
11. `✅ готово` **Метрики и оповещения** (2 д). Тики сервера, онлайн, сеть, ошибки — в
    журнал и на страницу; оповещение владельцу при падении.
    **Сделано (24.09):** метрики раз в окно — журнал, metrics.json, событие
    ресурсам, страница /metrics и /metrics.prom (по желанию, с токеном);
    оповещения в журнал, администраторам и на webhook: долгий тик, тикрейт,
    ошибки, память, зависание главного потока (сторож), аварийный перезапуск
    (метка running.json); свои метрики и оповещения геймода.
12. `🟡 код готов, в игре не проверено` **Отчёты о падениях клиента** (2 д). Мини-дамп и строка в журнал сервера:
    без этого причины вылетов у игроков не видны.
    **Сделано (24.09):** клиент при вылете пишет мини-дамп и сводку в
    `%LOCALAPPDATA%\FloVMP\crashes` (последние 5), при следующем входе шлёт
    серверу строку CRASH (код, модуль+смещение, версии); сервер — журнал,
    `flovmp-data/client-crashes.log`, счёт мест (`crashes` в консоли),
    оповещение при 5 одинаковых, событие `flovmp:client:crash`. Дамп остаётся
    у игрока. В игре не проверено (нужен настоящий вылет; строку сервера
    проверяет `bot.py --crash-test`).

## Фаза 3 — экосистема вокруг продукта (1–2 месяца)

13. `🟡 частично` **Веб-панель лицензий** (7 д). Сейчас ключи выдаются командой на VDS.
    Нужно: кабинет владельца (ключи, серверы, срок, отвязка), страница
    покупки, автоматическая выдача, письма о продлении.
    **Ход (26.09):** в `web/` есть кабинет, админка, API лицензий (продление,
    включение/выключение) и выставления счетов; идёт редизайн сайта.
    Выдача ключей после оплаты и письма о продлении — не проверены.
14. `🟡 частично` **Обновления по каналам** (3 д). stable/beta, кнопка «обновить» в панели
    сервера, откат на прошлую версию одной командой.
    **Ход (26.09):** подписанные релизы на VDS и обновление на месте есть;
    каналов stable/beta, кнопки в панели и отката одной командой — нет.
15. `⏸ отложено владельцем` **Лаунчер игрока** (10 д). Владелец 25.09: «лаунчер пока не нужен». Проверка версии клиента, список серверов
    владельца, установка модов сервера, вход по одной кнопке.
16. `🟡 частично` **SDK и документация** (5 д). Примеры геймодов (работа, магазин, дом),
    справочник событий, шаблон проекта, тесты для разработчика сервера.
    **Ход (26.09):** шаблон `gamemode`, справочник событий в его README,
    пример `client_packages` (нативы + HTML). Примеров «работа/магазин/дом»
    и тестов для разработчика сервера — нет.
17. `🟡 частично` **Перенос проектов с RAGE:MP и alt:V** (15 д суммарно). Самый сильный
    аргумент для продаж: у владельца сервера уже написан код, и переезд должен
    занимать дни, а не месяцы. Три части:

    - **Слой совместимости API** (8 д). Наши обёртки с именами, привычными по
      RAGE:MP (`mp.events.add`, `mp.players.local`, `player.call`) и alt:V
      (`alt.on`, `alt.emitClient`, `alt.Player`), поверх событий FloV:MP.
      Тогда большая часть серверного кода работает без правок, а не
      переписывается.
    - **Скрипт-переносчик** (5 д). `flovmp migrate --from ragemp <папка>`:
      читает проект, раскладывает ресурсы по нашей структуре, переписывает
      известные вызовы API, переносит конфиги, базу и карты, а всё, что не
      смог, собирает в отчёт `migration-report.md` со ссылками на файлы и
      строки. **Безопасность обязательна:** работает по копии, исходная папка
      не меняется, есть `--dry-run` (только отчёт, ничего не пишет), каждое
      изменение видно в diff, откат — удалением новой папки.
    - **Совместимость данных** (2 д). Таблицы игроков, аккаунтов, транспорта и
      домов из RAGE:MP/alt:V переносятся миграциями в нашу схему с проверкой
      целостности и отчётом «сколько строк перенесено, сколько пропущено и
      почему».

    **Ход (26.09):** клиентская часть API RAGE:MP готова (пункты 26a/26b:
    `mp.events`, `mp.game.*`, `mp.browsers`, `mp.keys`, `mp.gui`); серверный
    слой и переносчик — пункт 26c, не начаты.

    Что переносится автоматически, а что руками, будет видно после разбора
    одного реального проекта (у владельца есть SayonaraRP на RAGE:MP — на нём
    и проверим счёт: сколько процентов кода поехало без правок).

## Фаза 4 — масштаб (после первых продаж)

18. `⬜ не начато` **Нагрузочный тест на 1000+** (5 д). Боты, профиль тика, узкие места.
19. `⬜ не начато` **Разделение мира по зонам** (10 д). Несколько процессов на один мир,
    чтобы онлайн не упирался в один тик.
20. `⬜ не начато` **Голос по каналам** (5 д). Рации, телефон, группы — отдельно от радиуса.
21. `⬜ не начато` **Античит третьей линии** (10 д). Проверка целостности клиента, защита
    от инъекций, серверная валидация анимаций.

## Фаза 5 — ответ конкурентам (добавлено 24.09.2026)

Появился прямой конкурент — MiNT Multiplayer, первый релиз 11.09.2026, на том
же SDK alt:V, что и мы, с релизами раз в 5–6 дней. Разбор и таблица
расхождений: [рынок и конкуренты](market/competitors.md).

Пункты ниже — не копирование чужого, а закрытие дыр, которые стали
видны на фоне чужого списка возможностей. Самый важный — первый.

22. `🟡 частично` **Перехват урона, огня и взрыва геймодом** (4 д). Сейчас сервер сам
    считает здоровье и броню, но геймод не может сказать «этот выстрел не
    прошёл» или «урон вдвое меньше». Без этого не сделать ни брони фракций,
    ни режимов, ни дуэлей. Нужны события с правом отмены и изменения
    значения, включая рукопашную. Это самая большая разница с конкурентом.
    **Ход (24.09):** урон и рукопашная — код готов: после серверной проверки
    `HIT`, но до списания HP/брони ресурс получает `flovmp:damage` и может
    отменить попадание либо заменить значение через `flovmp:damage:set`;
    молчание ресурса сохраняет прежнее поведение, просроченный ответ не влияет
    на следующий выстрел. **Исправлено 25.09:** первая версия ждала ответ в
    том же вызове и не работала совсем — события между ресурсами alt:V
    доставляются после тика платформы, ответ всегда опаздывал. Теперь
    попадание ждёт решения один тик; проверено живым запуском с отдельным
    ресурсом-геймодом (урон 40 → 20 по ответу). Огонь и взрывы ещё не
    перехватываются — пункт закрыт частично и требует протокола клиента 3889.
    **22b — огонь и взрывы: протокол на согласовании** —
    `docs/fire-explosions-protocol.md`. Честно: урон от взрыва/огня игрока
    перехватывается как пули; сам эффект у стрелка не отменить (рождается в
    его GTA) — только не показать остальным и потушить после; сейчас взрывы
    и огонь другим игрокам не передаются вовсе.
23. `✅ готово` **Асинхронные обработчики в SDK** (4 д). Сейчас разработчик геймода
    уходит в базу синхронно и вешает тик всего сервера. Нужны обработчики,
    возвращающие `Task`, с токеном отмены при остановке ресурса и возвратом
    в контекст ресурса после `await`.
    **Сделано (25.09):** `FloVMP.Core/Async/MainThreadQueue` (контекст
    синхронизации ресурса: продолжения — в очередь, разбор в OnTick с
    бюджетом 4 мс, контекст ставится только на время своих вызовов — главный
    поток alt:V общий), `FloVMP.Sdk/FloVAsync` (`OnServerAsync` до 6 арг.,
    `Run`, `OnMainThread`, `NextTick`), шаблон gamemode подключён. Живой
    прогон двух ресурсов в одном сервере: пока геймод 300 мс «ходит в базу»,
    прошло 268 тиков, самый длинный промежуток 39–43 мс (синхронный
    обработчик — 309 мс простоя); после await — главный поток, вызов движка
    дошёл до другого ресурса; событие платформы обработано асинхронно;
    остановка ресурса во время await — finally выполнен, Stop 6 мс. Живой
    прогон нашёл две ошибки, которых не видели тесты (finally при остановке
    не выполнялся; успешные обработчики не снимались со счёта — остановка
    ждала 500 мс), обе исправлены и закрыты тестами.
24. `🟡 частично` **Межресурсный слой** (5 д). Экспорт функций с возвратом значения,
    типизированные метаданные, маркеры и 3D-надписи для одного игрока.
    Без этого крупный проект пишется одним комом в одном ресурсе.
    **Экспорты — сделано (25.09):** `FloVMP.Sdk/FloVExports` на штатных
    Alt.Export/Import. Сначала живая проба двух ресурсов: экспорты между
    C#-ресурсами синхронны (тот же вызов, поток и тик), Task<T> проходит; но
    штатно — Import бросает InvalidImportException (в OnStart роняет сервер),
    неверный тип молча приводится и падает FormatException при вызове, после
    перезапуска экспортёра старый импорт зовёт код прежнего экземпляра, а
    await экспортёра продолжается в очереди вызывающего. FloVExports это
    закрывает: подпись (`FloVMP.Core/Resources/ExportSignature`) сверяется до
    вызова, кэш сбрасывается по OnAnyResourceStop/Start, функция экспортёра —
    в его контексте, всё — FloVExportException. Проверено живым прогоном
    двух ресурсов (таблица — в коммите). Типизированные метаданные и
    маркеры/3D-надписи для одного игрока — отдельно: нужен клиент 3889
    (game.cpp) и согласование протокола.
25. `✅ готово` **Переход на .NET 10** (2 д). **Сделано (24.09):** Core, Starter и шаблон
    геймода собираются под net10.0; штатный host alt:V проверен с runtime
    10.0.10, установщики и упаковщик ставят/разрешают нужную major-версию.
26. `🟡 частично` **Полная кастомизация клиента, как в RAGE:MP** (добавлено 25.09 по
    требованию владельца). До этого «кастомизация клиента» была только
    настройками `client.cfg`: свой HUD или интерфейс владелец сервера сделать
    не мог. Три части:
    - `✅ готово` **26a — клиентский JS и client_packages. Сделано (25.09):** движок
      quickjs-ng в `FloVMP.asi`, API `mp.*` как в RAGE:MP (события, таймеры,
      клавиши, чат, курсор, `mp.players.local`, `require`), все 5176 нативов
      `mp.game.*` с выходными параметрами и Vector3, `invoke` по хэшу,
      `drawText`. Сервер раздаёт `server/client_packages` тем же HTTP, что и
      моды (область `/client/`, SHA-256, кэш у игрока), события
      `flovmp:client:call/callAll` ↔ `flovmp:client:event`, `reloadclient`.
      Изоляция: нет std/os, require только внутри пакета, код только с того
      же сервера, прерывание зависшего кода через 100 мс, память 256 МБ,
      лимиты событий в обе стороны. Тесты: сервер 624, движок 30 (включая
      пример из SDK).
    - `🟡 код и тесты готовы, в GTA не проверено` **26b — браузеры (CEF). Сделано (25.09):** Chromium 131 (CEF) в
      отдельном процессе `flovmp-cef.exe` (job-объект: закрылась игра —
      закрылся и он; упал — поднимается заново, страницы возвращаются).
      Кадры — разделяемая память с подтверждением и копированием только
      изменившейся области в текстуру DX11, смешивание с умноженной альфой;
      команды и события — две трубы. API как в RAGE:MP: `mp.browsers.new`,
      `call/execute/url/active/reload/destroy/markAsChat`, рабочие
      `orderId/inputEnabled/frameRate (1–60)`, события `browserCreated/DomReady` и
      `browserLoadingFailed(browser, code, url)`, `browserHostLost/Restored`,
      в странице `mp.trigger` и
      `mp.events`; `package://` из client_packages (UTF-8, без выхода за
      папку), `file://` запрещён. Курсор `mp.gui.cursor.show(freeze, show)`
      наконец реально работает: мышь по прозрачности стабильного кадра к нужной
      странице, клавиатура с кириллицей, заморозка персонажа. Слои: мир и
      ники → страницы → чат/меню/консоль платформы. Поставка: `FloVMP\cef`
      в комплекте игрока (255 МБ, лишнее вырезано), установщик и удаление.
      Bridge доступен только главному frame, не iframe; shared-memory кадр
      сначала копируется и проверяется, поэтому CEF не может отдать в GPU смесь
      двух кадров. Cookies/localStorage живут только в памяти и изолируются при
      смене server package. Chromium работает со штатным Windows sandbox;
      отдельный incognito root каждой жизни host исключает конфликт двух
      клиентов и быстрого восстановления через singleton-lock CEF. Команды
      `url/active/frameRate/destroy` не теряются,
      даже если пришли до асинхронного создания Chromium. CEF→client ограничен
      240 событиями/с на browser и 1000 суммарно; pipe-запись обрабатывает
      частичные WriteFile. Fault-injection убивает CEF и подтверждает restart,
      повторное создание страницы и текстуры. До сетевой policy тесты без GTA
      на настоящем Chromium проходили: браузеры 31, движок JS 37.
      Сеть fail-closed: без `browser-origins.txt` доступны только package/data/blob;
      внешний API/CDN/WebSocket требует точного origin из хэшируемого пакета,
      навигация, subresource, download и redirect проверяются отдельно. Remote
      main page получает `window.mp` только для разрешённого origin, iframe —
      никогда; browser process повторно проверяет источник сообщения. После
      сетевых regression-тестов: браузеры 36, движок JS 37. Ресурсы ограничены
      одинаково в клиенте и CEF host: до 12 browser и общий pixel-budget
      12×1920×1080. При перегрузке внутреннее CEF-разрешение безопасно снижается;
      `mp.browsers.stats` показывает слои, площадь, приблизительную память,
      upload/dropped frames и время загрузки кадров. С resource regression:
      браузеры 43, движок JS 37 (включая реальное снижение кадров на 1 FPS и
      остановку hidden browser). Полный API слоёв: per-browser physical-pixel
      `bounds/setBounds/resetBounds`, `focus/blur/focused`, responsive fullscreen,
      безопасный resize и lifecycle `browserDestroyed/Crashed/Restored`.
      SDK-пример показывает HUD + modal + transparent click-through и recovery.
      После этих regression-тестов: Chromium 47, QuickJS/API 42.
      **Проверка и исправления 26.09 (Claude, по отчёту Codex):** хост падал
      при закрытии страницы, пока Chromium её создаёт (new + destroy в одном
      кадре, отключение во время загрузки) — исправлено; восстановление
      больше не кончается после двух падений: паузы 1…30 с, больше 5 падений
      за 10 минут — `browserHostRecoveryFailed`; возвращён SwiftShader (без
      него GPU-процесс Chromium падал на каждом старте); стек падения хоста
      пишется в `cef-host.log`. Тесты: Chromium 56, QuickJS/API 43,
      healthcheck 73. **Не проверено в игре:** HUD, модальные окна, фокус,
      Alt+Tab, смена разрешения, DPI, восстановление после падения — нужен
      живой прогон в GTA 1.0.3889.
    - `⬜ не начато` **26c — серверный JS как в RAGE:MP** (`mp.*` на js-module alt:V) и
      скрипт переноса сервера с RAGE:MP (объединяется с пунктом 17).
27. `⬜ не начато` **Кость и часть тела в `flovmp:damage`** — чтобы геймод мог считать
    урон в голову/броню по частям тела. Нужен номер кости в `HIT` клиента 3889.
28. `⬜ не начато` **Резервный адрес сервера лицензий** — второй endpoint на случай
    недоступности основного VDS, без изменения ключей.
29. `✅ готово` **Лицензия проверяется и на стороне игрока** (26.09, по
    требованию владельца «слитый ключ не должен работать на чужом
    сервере»). Сервер лицензий подписывает подтверждение для игроков (без
    ключа: отпечаток лицензии, IP машины, проект, срок); сервер отдаёт его
    при входе, клиент проверяет подпись вшитым ключом, срок и адрес.
    Проверено на VDS и тестами (своя/чужая машина, LAN, просрочено,
    подделано). Остаётся: снизить лимит серверов у ключа заказчика (решение
    владельца), уведомления о новых активациях.
30. `⬜ не начато` **Транспорт шлюза 3889 по UDP** (добавлено 26.09). Сейчас
    шлюз — TCP. Сеть хостинга текущего VDS режет частые TCP-пакеты на
    игровых портах через 5–20 с (проверено: на самом VDS через 127.0.0.1 и
    к стороннему эхо-серверу всё держится) — клиент переподключается каждые
    ~23 с, в игре это рывки. Игровые хостинги с антиDDoS рассчитаны на UDP,
    как у RAGE:MP и alt:V. Нужен UDP с надёжной доставкой для команд и
    ненадёжной для движения; до тех пор — попросить хостинг открыть TCP
    7788/7798 без фильтра или сменить VDS.

---

## Известные ограничения, которые честно озвучиваем клиенту

- Раздача модов реализована сервером и `play.cmd`, но ещё не проверена живым
  запуском GTA; для загрузки папки `mods` игроку нужен OpenIV.asi (пункт 4).
- Штатное меню GTA на Esc останавливает игру — поэтому по умолчанию мы
  открываем своё меню; карта пока штатная (пункт 2).
- Серверный реестр транспорта и сохранение готовы, но финальный двухклиентский
  тест ownership migration в GTA ещё не проведён (пункт 8).
- Браузеры (CEF) проверены тестами на настоящем Chromium, но не внутри GTA
  (пункт 26b).
- Нагрузка проверена на десятках игроков, не на тысячах (пункт 18).
- Юридический риск платформы поверх бинарников alt:V владельцем принят.
