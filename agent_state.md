# Agent State — FloV:MP

Updated: 2026-08-27

## Current task
- [ ] Фаза 1 — минимальный скелет (2 игрока видят друг друга, двигаются)

## Queue
| # | Задача | Статус |
|---|------|--------|
| 0 | Проверить, стартует ли `altv-server.exe` автономно без бэкенда alt:V | ✅ done — стартует чисто, HTTP-порт живой |
| 1 | Найти/восстановить формат `server.toml`/`resources.cfg` (в бэкапе нет ни одного примера конфига) | todo |
| 2 | Минимальный C#-ресурс (`coreclr-module`) — hello world, загружается ли вообще | todo |
| 3 | Минимальный JS/TS клиентский ресурс — тест `js-module` | todo |
| 4 | Реальный тест подключения живым клиентом GTA V | todo, нужен реальный игровой клиент, не тестируется автоматически |
| 5 | Лаунчер — скелет WPF-проекта | todo |

## Done
- Разведка `C:\ViMP backup\backup-altv\` — подтверждено: чистый движок alt:V
  (клиент/сервер/coreclr-module/js-module/лаунчер/voice-server), НОЛЬ
  игрового кода внутри. `.bin`-файлы данных (клоузы/педы/машины/оружие)
  нужно копировать в рабочую папку сервера вручную — сам движок их не находит
  по дефолтному пути.
- Официальный лаунчер alt:V (`launcher/release/x64_win32/altv.exe`)
  **нерабочий** — падает на `curl code 6` (DNS не резолвится) при попытке
  достучаться до мёртвого `global cdn`. Не наша проблема — мы не используем
  их лаунчер, свой пишем с нуля (см. `launcher/`).
- Репозиторий создан, `CLAUDE.md` с полным контекстом решений от 2026-08-27
  (стек, MVP-критерии, брендинг) записан.

## Blockers
- Нет ни одного примера/референса формата серверного конфига (`server.toml`
  или аналог) — сервер стартовал только на дефолтах, реальный конфиг ещё не
  писали. Нужно найти в открытых источниках (старая публичная документация
  alt:V, если ещё доступна где-то в архивах/зеркалах) или методом проб.

## Notes
- Полная история решений/интервью с владельцем перед стартом — в истории
  чата Florida V-сессии от 2026-08-27 (там же откуда взят весь контекст
  выше). Если нужны точные детали (например, точный текст лога живого теста
  сервера) — спросить владельца, не гадать по памяти этого файла.
